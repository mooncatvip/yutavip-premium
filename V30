-- [[ YUTA HUB v30.0 - THE FINAL MASTERPIECE ]]
-- [[ DESIGN: 1:1 BANANA REPLICA | BRAND: YUTA HUB | EFFECTS: TRANSPARENT & BLURRED STROKE ]]

local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local YutaHub = Instance.new("ScreenGui", game.CoreGui)
local Main = Instance.new("Frame", YutaHub)
local Sidebar = Instance.new("Frame", Main)
local PageContainer = Instance.new("Frame", Main)
local LogoToggle = Instance.new("TextButton", YutaHub)

-- 1. LOGO YV TRÒN ĐỎ TRẮNG (CÓ THỂ DI CHUYỂN)
LogoToggle.Name = "YV_Logo_Toggle"
LogoToggle.Size = UDim2.new(0, 50, 0, 50)
LogoToggle.Position = UDim2.new(0, 25, 1, -80)
LogoToggle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
LogoToggle.Text = "YV"
LogoToggle.TextColor3 = Color3.fromRGB(255, 0, 0)
LogoToggle.Font = Enum.Font.GothamBold
LogoToggle.TextSize = 22
Instance.new("UICorner", LogoToggle).CornerRadius = UDim.new(1, 0)
local LStroke = Instance.new("UIStroke", LogoToggle)
LStroke.Color = Color3.fromRGB(255, 0, 0)
LStroke.Thickness = 2

-- LOGIC DRAG MOBILE CHO LOGO (FIX LỖI)
local draggingLogo, dragStart, startPos
LogoToggle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        draggingLogo = true dragStart = input.Position startPos = LogoToggle.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if draggingLogo and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        LogoToggle.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
LogoToggle.InputEnded:Connect(function() draggingLogo = false end)

-- 2. KHUNG MENU (BANANA STRUCTURE)
Main.Name = "GhostMainFrame"
Main.Size = UDim2.new(0, 580, 0, 340)
Main.Position = UDim2.new(0.5, -290, 0.5, -170)
Main.BackgroundColor3 = Color3.fromRGB(12, 12, 12) -- Nền tối Banana
Main.BackgroundTransparency = 0.2 -- Trong suốt
Main.Visible = false
Main.ClipsDescendants = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)

-- VIỀN MỜ BANANA STYLE
local MStroke = Instance.new("UIStroke", Main)
MStroke.Color = Color3.fromRGB(255, 200, 0) -- Giữ màu vàng Banana
MStroke.Thickness = 1.5
MStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

-- HEADER DI CHUYỂN MENU
local Header = Instance.new("Frame", Main)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundTransparency = 1
Header.ZIndex = 2
Header.InputBegan:Connect(function(input) -- Logic Kéo menu giống Banana
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        draggingMenu = true dragMenuStart = input.Position startMenuPos = Main.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if draggingMenu and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragMenuStart
        Main.Position = UDim2.new(startMenuPos.X.Scale, startMenuPos.X.Offset + delta.X, startMenuPos.Y.Scale, startMenuPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function() draggingMenu = false end)

-- REMAKE TÊN TITLE: YUTA HUB
local Title = Instance.new("TextLabel", Header)
Title.Text = "YUTA HUB : Blox Fruits"
Title.Size = UDim2.new(1, -20, 1, 0)
Title.Position = UDIM2.new(0, 15, 0, 0)
Title.TextColor3 = Color3.fromRGB(255, 200, 0) -- Giữ màu Banana
Title.Font = Enum.Font.GothamBold
Title.TextSize = 13
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.BackgroundTransparency = 1

-- 3. CẤU TRÚC SIDEBAR & PAGES (GIỐNG NGUYÊN LAYOUT BANANA)
Sidebar.Size = UDim2.new(0, 150, 1, -45)
Sidebar.Position = UDIM2.new(0, 10, 0, 40)
Sidebar.BackgroundTransparency = 1
Instance.new("UIListLayout", Sidebar).Padding = UDim.new(0, 4)

PageContainer.Size = UDIM2.new(1, -175, 1, -45)
PageContainer.Position = UDIM2.new(0, 170, 0, 40)
PageContainer.BackgroundTransparency = 1

local Pages = {}
local function AddTab(name)
    local P = Instance.new("ScrollingFrame", PageContainer)
    P.Size = UDim2.new(1, 0, 1, 0)
    P.BackgroundTransparency = 1
    P.Visible = false
    P.ScrollBarThickness = 0
    Instance.new("UIListLayout", P).Padding = UDim.new(0, 8)
    
    local TabBtn = Instance.new("TextButton", Sidebar)
    TabBtn.Size = UDim2.new(1, 0, 0, 35)
    TabBtn.Text = "  " .. name
    TabBtn.TextColor3 = Color3.fromRGB(180, 180, 180) -- Màu chữ chưa chọn Banana
    TabBtn.Font = Enum.Font.GothamMedium
    TabBtn.TextXAlignment = Enum.TextXAlignment.Left
    TabBtn.BackgroundTransparency = 1
    
    local Indicator = Instance.new("Frame", TabBtn) -- Thanh dọc vàng Banana
    Indicator.Size = UDim2.new(0, 2, 1, -10)
    Indicator.Position = UDim2.new(0, 0, 0.5, -((35-10)/2))
    Indicator.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
    Indicator.Visible = false

    TabBtn.MouseButton1Click:Connect(function()
        for _, v in pairs(Pages) do v.Visible = false end
        P.Visible = true
        for _, t in pairs(Sidebar:GetChildren()) do if t:IsA("TextButton") then t.TextColor3 = Color3.fromRGB(180, 180, 180) t.Frame.Visible = false end end
        TabBtn.TextColor3 = Color3.fromRGB(255, 200, 0) -- Màu Banana khi chọn
        Indicator.Visible = true
    end)
    Pages[name] = P
    return P
end

-- 4. SETUP NỘI DUNG (GIỐNG 100%)
local FarmTab = AddTab("Farm")
AddTab("Config")
AddTab("Fighting Style") -- Thêm các Tab cho đủ 100%
AddTab("Items Farm")
AddTab("Sea Events")
AddTab("Mirage + RaceV4") -- Tab riêng theo ý YUTA
AddTab("Drago Dojo")
AddTab("Tập kích")
AddTab("Music Player") -- Tab riêng theo ý YUTA

-- TAB FARM - SECTION FARM BANANA
local function CreateSection(parent, title)
    local SecFrame = Instance.new("Frame", parent)
    SecFrame.Size = UDim2.new(1, -10, 0, 30)
    SecFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25) -- Màu ô Banana
    SecFrame.BackgroundTransparency = 0.3 -- Làm trong suốt Section
    SecFrame.AutomaticSize = Enum.AutomaticSize.Y
    Instance.new("UICorner", SecFrame).CornerRadius = UDim.new(0, 5)
    
    local THeading = Instance.new("TextLabel", SecFrame)
    THeading.Size = UDim2.new(1, 0, 0, 25)
    THeading.Text = title
    THeading.TextColor3 = Color3.fromRGB(255, 200, 0) -- Giữ màu vàng Banana
    THeading.Font = Enum.Font.GothamBold
    THeading.TextSize = 12
    THeading.BackgroundTransparency = 1
    
    local ContentList = Instance.new("UIListLayout", SecFrame)
    ContentList.Padding = UDim.new(0, 5)
    ContentList.HorizontalAlignment = Enum.HorizontalAlignment.Center
    return SecFrame
end

-- HÀM TẠO TOGGLE (GIỐNG 100% NÚT BANANA)
local function AddToggle(parent, text)
    local TglBtn = Instance.new("TextButton", parent)
    TglBtn.Size = UDIM2.new(1, -20, 0, 35)
    TglBtn.Position = UDim2.new(0, 10, 0, 0)
    TglBtn.BackgroundTransparency = 1
    TglBtn.Text = "  " .. text
    TglBtn.TextColor3 = Color3.fromRGB(255, 255, 255) -- Màu trắng Banana
    TglBtn.Font = Enum.Font.GothamMedium
    TglBtn.TextSize = 13
    TglBtn.TextXAlignment = Enum.TextXAlignment.Left

    local ToggleBox = Instance.new("Frame", TglBtn)
    ToggleBox.Size = UDIM2.new(0, 18, 0, 18)
    ToggleBox.Position = UDIM2.new(1, -25, 0.5, -9)
    ToggleBox.BackgroundColor3 = Color3.fromRGB(45, 45, 45) -- Nút vuông Banana
    Instance.new("UICorner", ToggleBox).CornerRadius =
