-- MOON Hub | Blox Fruits - Purple Moon Theme
-- GUI kieu W-Azure / Maru - Khong emoji

repeat task.wait() until game:IsLoaded()

-- Load Redz Lib (link dang hoat dong 2026)
local success, RedzLib = pcall(loadstring, game:HttpGet("https://raw.githubusercontent.com/tlredz/Scripts/refs/heads/main/main.luau"))

if not success then
    RedzLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/realredz/BloxFruits/refs/heads/main/Source.lua"))()
end

local Window = RedzLib:MakeWindow({
    Name = "MOON Hub | Blox Fruits",
    HidePremium = false,
})

-- Tao cac tab
local FarmTab      = Window:MakeTab({Name = "Farm"})
local SeaTab       = Window:MakeTab({Name = "Sea Events"})
local RaceTab      = Window:MakeTab({Name = "Race & Drago"})
local RaidTab      = Window:MakeTab({Name = "Raid"})
local ItemsTab     = Window:MakeTab({Name = "Items"})
local TeleportTab  = Window:MakeTab({Name = "Teleport"})
local FruitTab     = Window:MakeTab({Name = "Fruit"})
local ESPTab       = Window:MakeTab({Name = "ESP"})
local MiscTab      = Window:MakeTab({Name = "Misc"})

-- Farm Tab
FarmTab:AddToggle({
    Name = "Auto Farm Level + Nhiem Vu",
    Default = false,
    Callback = function(v) getgenv().AutoFarmLevel = v end,
})

FarmTab:AddToggle({
    Name = "Auto Farm Bone + Katakuri",
    Default = false,
    Callback = function(v) getgenv().AutoBoneKatakuri = v end,
})

FarmTab:AddToggle({
    Name = "Auto Haki Rainbow",
    Default = false,
    Callback = function(v) getgenv().RainbowHaki = v end,
})

FarmTab:AddToggle({
    Name = "Auto Factory (Sea 2)",
    Default = false,
    Callback = function(v) getgenv().AutoFactory = v end,
})

FarmTab:AddToggle({
    Name = "Auto Hai Tac (Sea 3)",
    Default = false,
    Callback = function(v) getgenv().AutoPirateSea3 = v end,
})

-- Raid Tab
RaidTab:AddDropdown({
    Name = "Chon Raid",
    Options = {"Default", "Lua", "Bang", "Anh Sang", "Dung Nham", "Nhen", "Chan Dong", "Bong Toi", "Phuong Hoang", "Bot"},
    Default = "Default",
    Callback = function(v) getgenv().SelectedRaid = v end,
})

RaidTab:AddToggle({
    Name = "Auto Raid + Kill Aura",
    Default = false,
    Callback = function(v) getgenv().AutoRaid = v end,
})

-- Misc Tab
MiscTab:AddToggle({
    Name = "Always Day",
    Default = false,
    Callback = function(v) 
        if v then game.Lighting.ClockTime = 14 end 
    end,
})

MiscTab:AddToggle({
    Name = "Walk On Water",
    Default = false,
    Callback = function(v) getgenv().WaterWalk = v end,
})

MiscTab:AddSlider({
    Name = "Kill Aura Range",
    Min = 50,
    Max = 300,
    Default = 100,
    Increment = 25,
    Callback = function(v) getgenv().AuraRange = v end,
})

-- Main loop (co ban)
task.spawn(function()
    while task.wait(0.2) do
        pcall(function()
            if getgenv().AutoFarmLevel or getgenv().AutoRaid then
                -- Them logic farm + combat o day (can hook tu game)
            end
        end)
    end
end)

RedzLib:Notify({
    Title = "MOON Hub",
    Content = "Load thanh cong! GUI kieu W-Azure. Chon tinh nang va farm.",
    Duration = 5,
})
